import { NavBgChangeDirective } from './nav-bg-change.directive';

describe('NavBgChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBgChangeDirective();
    expect(directive).toBeTruthy();
  });
});
